import numpy as np
import pandas as pd

class Property:
    def solubility(self, temperature: float):
        """
        Compute solubility (kg solute in kg solvent) given celsius temperature
        :param temperature:
        :return:
        """

    def raman_concentration(self, input_data: pd.DataFrame) -> pd.DataFrame:
        """
        Given raman spectroscopy, calculate concentration in kg solute in kg solvent.
        :param raman:
        :return:
        """